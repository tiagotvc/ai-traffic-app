import { AdSetsHubClient } from "@/components/AdSetsHubClient";

export default function AdSetsPage() {
  return (
    <div className="-mx-6 -my-6 flex min-h-[calc(100vh-3rem)] lg:-mx-8 lg:-my-7">
      <AdSetsHubClient />
    </div>
  );
}
