/**
 * sessionStorage key used to carry the plan selected in the stack-cost
 * comparison picker down to the pricing section, so the matching card can
 * scroll into view and highlight itself instead of the user having to
 * re-scan the grid after jumping to `#pricing`.
 */
export const PRICING_HIGHLIGHT_KEY = "orion:marketing:pricing-highlight";
